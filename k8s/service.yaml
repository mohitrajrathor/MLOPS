apiVersion: v1
kind: Service

metadata:
  name: iris-inference-service
  
spec:
  type: LoadBalancer
  selector:
    app: iris-inference
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8100
